import { Component } from '@angular/core';

/**
 * This component is responsible for the deletion of a correspondence status.
 * It is a standalone component without any additional modules or complex logic.
 */
@Component({
  selector: 'app-delete-correspondence-status', // The component selector to use in HTML
  standalone: true, // This component is standalone and doesn't need to be declared in a module
  imports: [], // No external modules are imported
  templateUrl: './delete-correspondence-status.component.html', // Path to the HTML template
  styleUrls: ['./delete-correspondence-status.component.css'] // Path to the CSS file
})
export class DeleteCorrespondenceStatusComponent {
  // This component does not currently have any specific logic or methods
}
